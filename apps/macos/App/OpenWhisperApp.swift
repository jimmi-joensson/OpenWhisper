import SwiftUI

@main
struct OpenWhisperApp: App {
    var body: some Scene {
        WindowGroup("OpenWhisper") {
            ContentView()
        }
        .defaultSize(width: 580, height: 520)
    }
}
