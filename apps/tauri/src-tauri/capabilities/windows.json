{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "windows-only",
  "description": "Windows-only permissions: global shortcut for Ctrl+Space activation",
  "platforms": ["windows"],
  "windows": ["main", "pill"],
  "permissions": [
    "global-shortcut:default"
  ]
}
