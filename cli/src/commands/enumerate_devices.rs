//! `openwhisper enumerate-devices` — placeholder until Task 6.

use anyhow::{bail, Result};

pub fn run(_json: bool) -> Result<()> {
    bail!("`enumerate-devices` lands in TASK-81.2 / Task 6 — see backlog/tasks/task-81.6*")
}
