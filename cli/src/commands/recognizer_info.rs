//! `openwhisper recognizer-info` — placeholder until Task 7.

use anyhow::{bail, Result};

pub fn run(_json: bool) -> Result<()> {
    bail!("`recognizer-info` lands in TASK-81.2 / Task 7 — see backlog/tasks/task-81.7*")
}
